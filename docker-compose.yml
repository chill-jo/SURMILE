version: '3'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    restart: always
    container_name: survey-app
    environment:
      - REDIS_HOST=redis

  redis:
    image: redis/redis-stack:latest
    ports:
      - "6379:6379"
    container_name: redis